# Supabase Postgres connection string.
# Prefer Supabase's pooled connection URL for Vercel/serverless deployments.
DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require"

# Supabase Auth values. The VITE_ values are designed to be exposed to the browser.
VITE_SUPABASE_URL="https://PROJECT_REF.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_replace_me"

# Private Vercel Blob store for customer handover photos. Vercel creates this
# automatically when a private Blob store is connected to the project.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_replace_me"

# Comma-separated email addresses allowed to use the application.
ADMIN_EMAILS="admin@example.com"

# Keep both values false while the application is shared without login.
# Set both values to true and redeploy when login is required again.
AUTH_REQUIRED="false"
VITE_AUTH_REQUIRED="false"

# Calendar-derived task automation. Keep both switches false until the
# staging dry run and generated task have been checked.
CALENDAR_AUTOMATION_ENABLED="false"
VITE_CALENDAR_AUTOMATION_ENABLED="false"
CRON_SECRET="replace-with-a-long-random-secret"
GOOGLE_CALENDAR_SERVICE_ACCOUNT_EMAIL="calendar-reader@example.iam.gserviceaccount.com"
GOOGLE_CALENDAR_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nreplace-me\n-----END PRIVATE KEY-----\n"

# JSON array of private rule records. Do not commit customer details here.
CALENDAR_AUTO_TASK_RULES_JSON="[]"

# Optional analytics placeholders referenced by client/index.html.
VITE_ANALYTICS_ENDPOINT=""
VITE_ANALYTICS_WEBSITE_ID=""

# Required to upload AtInn handover Before/After photos. These server-only
# credentials are provided by the existing storage proxy and must never use a
# VITE_ prefix or be committed to Git.
BUILT_IN_FORGE_API_URL=""
BUILT_IN_FORGE_API_KEY=""
