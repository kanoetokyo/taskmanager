// Generated by scripts/build-vercel-api.mjs. Do not edit by hand.

// server/calendarTaskCron.ts
import { timingSafeEqual } from "node:crypto";
import { createRemoteJWKSet, jwtVerify } from "jose";

// server/calendarAutomation.ts
import { createHash } from "node:crypto";
import { and, eq as eq2 } from "drizzle-orm";
import { importPKCS8, SignJWT } from "jose";
import { z } from "zod";

// drizzle/schema.ts
import {
  bigint,
  boolean,
  index,
  integer,
  jsonb,
  pgEnum,
  pgTable,
  serial,
  text,
  timestamp,
  uniqueIndex,
  varchar
} from "drizzle-orm/pg-core";
var userRole = pgEnum("role", ["user", "admin"]);
var users = pgTable("users", {
  /**
   * Surrogate primary key. Auto-incremented numeric value managed by the database.
   * Use this for relations between tables.
   */
  id: serial("id").primaryKey(),
  /** Manus OAuth identifier (openId) returned from the OAuth callback. Unique per user. */
  openId: varchar("openId", { length: 64 }).notNull().unique(),
  name: text("name"),
  email: varchar("email", { length: 320 }),
  loginMethod: varchar("loginMethod", { length: 64 }),
  role: userRole("role").default("user").notNull(),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date()),
  lastSignedIn: timestamp("lastSignedIn").defaultNow().notNull()
});
var taskStates = pgTable(
  "task_states",
  {
    id: serial("id").primaryKey(),
    dateKey: varchar("dateKey", { length: 10 }).notNull(),
    taskId: varchar("taskId", { length: 128 }).notNull(),
    done: boolean("done").default(false).notNull(),
    help: boolean("help").default(false).notNull(),
    note: varchar("note", { length: 1024 }).notNull().default(""),
    planned: varchar("planned", { length: 64 }).notNull().default(""),
    revision: integer("revision").notNull().default(1),
    updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
  },
  (table) => ({
    dateKeyTaskIdIdx: uniqueIndex("task_states_date_task_unique").on(
      table.dateKey,
      table.taskId
    )
  })
);
var calendarAutoTasks = pgTable(
  "calendar_auto_tasks",
  {
    id: varchar("id", { length: 64 }).primaryKey(),
    ruleId: varchar("ruleId", { length: 128 }).notNull(),
    targetMonth: varchar("targetMonth", { length: 7 }).notNull(),
    sourceEventId: varchar("sourceEventId", { length: 512 }),
    dateKey: varchar("dateKey", { length: 10 }).notNull(),
    category: varchar("category", { length: 128 }).notNull(),
    label: varchar("label", { length: 512 }).notNull(),
    defaultPlanned: varchar("defaultPlanned", { length: 64 }).notNull().default(""),
    status: varchar("status", { length: 32 }).notNull().default("scheduled"),
    details: jsonb("details").notNull().default({}),
    revision: integer("revision").notNull().default(1),
    createdAt: timestamp("createdAt").defaultNow().notNull(),
    updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
  },
  (table) => ({
    ruleMonthIdx: uniqueIndex("calendar_auto_tasks_rule_month_unique").on(
      table.ruleId,
      table.targetMonth
    ),
    dateStatusIdx: index("calendar_auto_tasks_date_status_idx").on(
      table.dateKey,
      table.status
    )
  })
);
var storeCheckStates = pgTable(
  "store_check_states",
  {
    id: serial("id").primaryKey(),
    dateKey: varchar("dateKey", { length: 10 }).notNull(),
    checkType: varchar("checkType", { length: 32 }).notNull(),
    checkedStores: jsonb("checkedStores").notNull().$type().default([]),
    revision: integer("revision").notNull().default(1),
    updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
  },
  (table) => ({
    dateKeyCheckTypeIdx: uniqueIndex("store_check_states_date_type_unique").on(
      table.dateKey,
      table.checkType
    )
  })
);
var individualHandovers = pgTable("individual_handovers", {
  id: varchar("id", { length: 64 }).primaryKey(),
  dateKey: varchar("dateKey", { length: 10 }).notNull(),
  author: varchar("author", { length: 64 }).notNull().default(""),
  target: varchar("target", { length: 64 }).notNull().default(""),
  tasks: jsonb("tasks").notNull().$type().default([]),
  completed: boolean("completed").default(false).notNull(),
  completedAt: timestamp("completedAt"),
  deletedAt: timestamp("deletedAt"),
  important: boolean("important").default(false).notNull(),
  updatedBy: varchar("updatedBy", { length: 64 }).notNull().default(""),
  revision: integer("revision").notNull().default(1),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});
var customerHandovers = pgTable("customer_handovers", {
  id: varchar("id", { length: 64 }).primaryKey(),
  dateKey: varchar("dateKey", { length: 10 }).notNull(),
  customerName: varchar("customerName", { length: 128 }).notNull().default(""),
  store: varchar("store", { length: 64 }).notNull().default(""),
  content: varchar("content", { length: 2048 }).notNull().default(""),
  status: varchar("status", { length: 32 }).notNull().default("\u5BFE\u5FDC\u4E2D"),
  assignee: varchar("assignee", { length: 64 }).notNull().default(""),
  links: jsonb("links").$type(),
  dueDate: bigint("dueDate", { mode: "number" }),
  callCount: integer("callCount").default(0).notNull(),
  completedAt: timestamp("completedAt"),
  deletedAt: timestamp("deletedAt"),
  updatedBy: varchar("updatedBy", { length: 64 }).notNull().default(""),
  revision: integer("revision").notNull().default(1),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});
var auditLogs = pgTable("audit_logs", {
  id: serial("id").primaryKey(),
  entityType: varchar("entityType", { length: 64 }).notNull(),
  entityId: varchar("entityId", { length: 128 }).notNull(),
  action: varchar("action", { length: 32 }).notNull(),
  before: jsonb("before").$type(),
  after: jsonb("after").$type(),
  actorId: varchar("actorId", { length: 64 }),
  requestId: varchar("requestId", { length: 128 }),
  createdAt: timestamp("createdAt").defaultNow().notNull()
});
var misocaStatus = pgTable("misoca_status", {
  id: serial("id").primaryKey(),
  completedUntil: varchar("completedUntil", { length: 16 }).notNull().default(""),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});
var grayCellStatus = pgTable("gray_cell_status", {
  id: serial("id").primaryKey(),
  confirmedUntil: varchar("confirmedUntil", { length: 16 }).notNull().default(""),
  updatedBy: varchar("updatedBy", { length: 64 }).notNull().default(""),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});
var storesShiftStatus = pgTable("stores_shift_status", {
  id: serial("id").primaryKey(),
  confirmedUntil: varchar("confirmedUntil", { length: 16 }).notNull().default(""),
  updatedBy: varchar("updatedBy", { length: 64 }).notNull().default(""),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});
var taskCategories = pgTable("task_categories", {
  id: serial("id").primaryKey(),
  name: varchar("name", { length: 128 }).notNull(),
  sortOrder: integer("sortOrder").notNull().default(0),
  isActive: boolean("isActive").notNull().default(true),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});
var taskDefinitions = pgTable("task_definitions", {
  id: serial("id").primaryKey(),
  categoryId: integer("categoryId").notNull(),
  label: varchar("label", { length: 512 }).notNull(),
  defaultPlanned: varchar("defaultPlanned", { length: 64 }).notNull().default("\u5F53\u65E5\u4E8B\u52D9\u62C5\u5F53"),
  deadline: varchar("deadline", { length: 64 }).notNull().default(""),
  sortOrder: integer("sortOrder").notNull().default(0),
  isActive: boolean("isActive").notNull().default(true),
  legacyId: varchar("legacyId", { length: 128 }),
  // 既存BASE_TASKS文字列IDとの互換性維持用
  showOnDays: varchar("showOnDays", { length: 128 }).notNull().default(""),
  // 表示日制限（例: "15,30" = 毎月15日・30日のみ。空文字列 = 常時表示）
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().notNull().$onUpdate(() => /* @__PURE__ */ new Date())
});

// server/db.ts
import { eq } from "drizzle-orm";
import { drizzle } from "drizzle-orm/postgres-js";
import postgres from "postgres";

// server/_core/env.ts
var ENV = {
  appId: process.env.VITE_APP_ID ?? "",
  cookieSecret: process.env.JWT_SECRET ?? "",
  databaseUrl: process.env.DATABASE_URL ?? "",
  oAuthServerUrl: process.env.OAUTH_SERVER_URL ?? "",
  ownerOpenId: process.env.OWNER_OPEN_ID ?? "",
  isProduction: process.env.NODE_ENV === "production",
  forgeApiUrl: process.env.BUILT_IN_FORGE_API_URL ?? "",
  forgeApiKey: process.env.BUILT_IN_FORGE_API_KEY ?? ""
};

// server/db.ts
var _db = null;
var _client = null;
async function getDb() {
  if (!_db && process.env.DATABASE_URL) {
    try {
      _client = postgres(process.env.DATABASE_URL, {
        max: 5,
        prepare: false
      });
      _db = drizzle(_client);
    } catch (error) {
      console.warn("[Database] Failed to connect:", error);
      _db = null;
      _client = null;
    }
  }
  return _db;
}

// server/calendarAutomation.ts
var JAPAN_TIME_ZONE = "Asia/Tokyo";
var GOOGLE_TOKEN_URL = "https://oauth2.googleapis.com/token";
var GOOGLE_CALENDAR_SCOPE = "https://www.googleapis.com/auth/calendar.readonly";
var ruleSchema = z.object({
  id: z.string().min(1).max(128),
  calendarId: z.string().min(1).max(512),
  customerMatch: z.object({
    descriptionMustContain: z.array(z.string().min(1).max(512)).min(3).max(8),
    summaryMustContain: z.array(z.string().min(1).max(128)).max(3).optional()
  }),
  officePresence: z.object({
    titleContainsAny: z.array(z.string().min(1).max(128)).min(1).max(8),
    colorId: z.string().min(1).max(16),
    searchBackDays: z.number().int().min(1).max(7).default(7),
    searchForwardDays: z.number().int().min(0).max(7).optional()
  }),
  task: z.object({
    title: z.string().min(1).max(512),
    category: z.string().min(1).max(128),
    defaultPlanned: z.string().max(64).default("\u5F53\u65E5\u4E8B\u52D9\u62C5\u5F53")
  })
});
var googleEventSchema = z.object({
  id: z.string().min(1),
  summary: z.string().optional().default(""),
  description: z.string().optional().default(""),
  status: z.string().optional().default("confirmed"),
  colorId: z.string().optional(),
  start: z.object({
    date: z.string().optional(),
    dateTime: z.string().optional()
  }),
  end: z.object({
    date: z.string().optional(),
    dateTime: z.string().optional()
  }).optional()
});
function normalizeForMatch(value) {
  return value.normalize("NFKC").toLocaleLowerCase("ja-JP").replace(/[\s\-‐‑‒–—―ー]/g, "");
}
function dateKeyFromParts(year, month, day) {
  return `${year}-${String(month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
}
function dateKeyInJapan(value) {
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return null;
  const parts = new Intl.DateTimeFormat("en-CA", {
    timeZone: JAPAN_TIME_ZONE,
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  }).formatToParts(date);
  const values = Object.fromEntries(
    parts.filter((part) => part.type !== "literal").map((part) => [part.type, part.value])
  );
  return dateKeyFromParts(
    Number(values.year),
    Number(values.month),
    Number(values.day)
  );
}
function dateKeyForNow(now) {
  return dateKeyInJapan(now.toISOString()) ?? "";
}
function shiftDateKey(dateKey, days) {
  const [year, month, day] = dateKey.split("-").map(Number);
  const date = new Date(Date.UTC(year, month - 1, day));
  date.setUTCDate(date.getUTCDate() + days);
  return dateKeyFromParts(
    date.getUTCFullYear(),
    date.getUTCMonth() + 1,
    date.getUTCDate()
  );
}
function monthForDateKey(dateKey) {
  return dateKey.slice(0, 7);
}
function addMonths(targetMonth, amount) {
  const [year, month] = targetMonth.split("-").map(Number);
  const date = new Date(Date.UTC(year, month - 1 + amount, 1));
  return `${date.getUTCFullYear()}-${String(date.getUTCMonth() + 1).padStart(2, "0")}`;
}
function eventStartDateKey(event) {
  if (event.start.date) return event.start.date;
  return event.start.dateTime ? dateKeyInJapan(event.start.dateTime) : null;
}
function eventEndDateKey(event) {
  if (!event.end) return eventStartDateKey(event);
  if (event.end.date) return shiftDateKey(event.end.date, -1);
  return event.end.dateTime ? dateKeyInJapan(event.end.dateTime) : eventStartDateKey(event);
}
function eventCoversDate(event, dateKey) {
  const start = eventStartDateKey(event);
  const end = eventEndDateKey(event);
  return Boolean(start && end && start <= dateKey && dateKey <= end);
}
function eventMatchesCustomer(event, rule) {
  if (event.status === "cancelled") return false;
  const description = normalizeForMatch(event.description);
  const summary = normalizeForMatch(event.summary);
  return rule.customerMatch.descriptionMustContain.every(
    (value) => includesConfiguredCustomerValue(description, normalizeForMatch(value))
  ) && (rule.customerMatch.summaryMustContain ?? []).every(
    (value) => includesConfiguredCustomerValue(summary, normalizeForMatch(value))
  );
}
function calendarSyncDiagnostic(rule, events, targetMonth) {
  const activeEvents = events.filter((event) => event.status !== "cancelled");
  const matchValues = rule.customerMatch.descriptionMustContain.map(
    (value) => normalizeForMatch(value)
  );
  const fieldMatches = (event) => {
    const description = normalizeForMatch(event.description);
    return matchValues.map(
      (value) => includesConfiguredCustomerValue(description, value)
    );
  };
  const matchingVisits = activeEvents.filter(
    (event) => eventStartDateKey(event)?.startsWith(targetMonth) && eventMatchesCustomer(event, rule)
  );
  return {
    ruleId: rule.id,
    fetchedEventCount: events.length,
    targetMonthEventCount: activeEvents.filter(
      (event) => eventStartDateKey(event)?.startsWith(targetMonth)
    ).length,
    configuredMatchHashes: matchValues.map(
      (value) => createHash("sha256").update(value).digest("hex").slice(0, 12)
    ),
    matchingFieldCounts: matchValues.map((value) => {
      return activeEvents.filter(
        (event) => includesConfiguredCustomerValue(
          normalizeForMatch(event.description),
          value
        )
      ).length;
    }),
    matchingVisitStartDates: matchingVisits.map((event) => eventStartDateKey(event)).filter((dateKey) => Boolean(dateKey)).sort(),
    candidateEvents: activeEvents.map((event) => ({ startDate: eventStartDateKey(event), matchedFields: fieldMatches(event) })).filter((candidate) => candidate.matchedFields.some(Boolean)).sort((left, right) => (left.startDate ?? "").localeCompare(right.startDate ?? "")).slice(-10)
  };
}
function includesConfiguredCustomerValue(description, configuredValue) {
  if (!configuredValue) return false;
  let index2 = description.indexOf(configuredValue);
  while (index2 !== -1) {
    const followingCharacter = description[index2 + configuredValue.length] ?? "";
    if (!/\d$/.test(configuredValue) || !/^\d/.test(followingCharacter)) {
      return true;
    }
    index2 = description.indexOf(configuredValue, index2 + configuredValue.length);
  }
  return false;
}
function hasOfficePresence(events, rule, dateKey) {
  return events.some((event) => {
    if (event.status === "cancelled") {
      return false;
    }
    if (event.colorId && event.colorId !== rule.officePresence.colorId) {
      return false;
    }
    const summary = normalizeForMatch(event.summary);
    return eventCoversDate(event, dateKey) && rule.officePresence.titleContainsAny.some(
      (value) => summary.includes(normalizeForMatch(value))
    );
  });
}
function eventSortValue(event) {
  return event.start.dateTime ?? `${event.start.date ?? ""}T00:00:00+09:00`;
}
function evaluateCalendarTaskRule(rule, events, targetMonth, runDateKey) {
  const visits = events.filter((event) => eventStartDateKey(event)?.startsWith(targetMonth)).filter((event) => eventMatchesCustomer(event, rule)).sort((left, right) => eventSortValue(left).localeCompare(eventSortValue(right)));
  const finalVisit = visits.at(-1);
  const finalVisitDate = finalVisit ? eventStartDateKey(finalVisit) : null;
  if (!finalVisit || !finalVisitDate) {
    return { kind: "no_matching_visit" };
  }
  const searchForwardDays = rule.officePresence.searchForwardDays;
  const offsets = searchForwardDays === void 0 ? Array.from(
    { length: rule.officePresence.searchBackDays },
    (_, index2) => -(index2 + 1)
  ) : Array.from({ length: searchForwardDays + 1 }, (_, index2) => index2);
  for (const offset of offsets) {
    const dateKey = shiftDateKey(finalVisitDate, offset);
    if (hasOfficePresence(events, rule, dateKey)) {
      return {
        kind: "task",
        sourceEventId: finalVisit.id,
        finalVisitDate,
        dateKey,
        label: rule.task.title,
        status: "scheduled"
      };
    }
  }
  return {
    kind: "task",
    sourceEventId: finalVisit.id,
    finalVisitDate,
    dateKey: runDateKey,
    label: `${rule.task.title}\uFF08\u4E8B\u52D9\u5728\u5E2D\u65E5\u8981\u78BA\u8A8D\uFF09`,
    status: "needs_review",
    reason: "no_office_presence"
  };
}
function getDefaultSyncMonth(now = /* @__PURE__ */ new Date()) {
  const currentMonth = monthForDateKey(dateKeyForNow(now));
  const currentDay = Number(dateKeyForNow(now).slice(-2));
  return currentDay === 21 ? addMonths(currentMonth, 1) : currentMonth;
}
function isCalendarAutomationEnabled() {
  return process.env.CALENDAR_AUTOMATION_ENABLED === "true";
}
function getCalendarTaskRules() {
  const raw = process.env.CALENDAR_AUTO_TASK_RULES_JSON;
  if (!raw) return [];
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    throw new Error("Calendar automation rule configuration is invalid.");
  }
  return z.array(ruleSchema).min(1).parse(parsed);
}
function selectCalendarTaskRules(rules, ruleId) {
  if (!ruleId) return rules;
  return rules.filter((rule) => rule.id === ruleId);
}
function calendarWindow(targetMonth) {
  const firstDay = `${targetMonth}-01`;
  const nextMonth = addMonths(targetMonth, 1);
  return {
    timeMin: `${shiftDateKey(firstDay, -7)}T00:00:00+09:00`,
    // Forward-scheduled tasks may need an office day in the following month.
    timeMax: `${shiftDateKey(`${nextMonth}-01`, 7)}T00:00:00+09:00`
  };
}
async function getGoogleAccessToken() {
  const clientEmail = process.env.GOOGLE_CALENDAR_SERVICE_ACCOUNT_EMAIL;
  const privateKey = process.env.GOOGLE_CALENDAR_SERVICE_ACCOUNT_PRIVATE_KEY;
  if (!clientEmail || !privateKey) {
    throw new Error("Google Calendar service account is not configured.");
  }
  const now = Math.floor(Date.now() / 1e3);
  const key = await importPKCS8(privateKey.replace(/\\n/g, "\n"), "RS256");
  const assertion = await new SignJWT({ scope: GOOGLE_CALENDAR_SCOPE }).setProtectedHeader({ alg: "RS256", typ: "JWT" }).setIssuer(clientEmail).setSubject(clientEmail).setAudience(GOOGLE_TOKEN_URL).setIssuedAt(now).setExpirationTime(now + 3600).sign(key);
  const response = await fetch(GOOGLE_TOKEN_URL, {
    method: "POST",
    headers: { "Content-Type": "application/x-www-form-urlencoded" },
    body: new URLSearchParams({
      grant_type: "urn:ietf:params:oauth:grant-type:jwt-bearer",
      assertion
    })
  });
  if (!response.ok) {
    const failure = await response.json().catch(() => null);
    const reason = failure && typeof failure.error === "string" ? failure.error : `HTTP ${response.status}`;
    throw new Error(`Google Calendar authorization failed (${reason}).`);
  }
  const payload = await response.json();
  if (!payload.access_token) throw new Error("Google Calendar token is missing.");
  return payload.access_token;
}
async function readCalendarEvents(calendarId, targetMonth) {
  const accessToken = await getGoogleAccessToken();
  const { timeMin, timeMax } = calendarWindow(targetMonth);
  const events = [];
  let pageToken;
  do {
    const url = new URL(
      `https://www.googleapis.com/calendar/v3/calendars/${encodeURIComponent(calendarId)}/events`
    );
    url.searchParams.set("timeMin", timeMin);
    url.searchParams.set("timeMax", timeMax);
    url.searchParams.set("singleEvents", "true");
    url.searchParams.set("orderBy", "startTime");
    url.searchParams.set("timeZone", JAPAN_TIME_ZONE);
    url.searchParams.set("maxResults", "2500");
    url.searchParams.set(
      "fields",
      "items(id,summary,description,status,colorId,start,end),nextPageToken"
    );
    if (pageToken) url.searchParams.set("pageToken", pageToken);
    const response = await fetch(url, {
      headers: { Authorization: `Bearer ${accessToken}` }
    });
    if (!response.ok) throw new Error("Google Calendar read failed.");
    const payload = await response.json();
    events.push(
      ...(payload.items ?? []).map((item) => googleEventSchema.safeParse(item)).filter((result) => result.success).map((result) => result.data)
    );
    pageToken = payload.nextPageToken;
  } while (pageToken);
  return events;
}
function generatedTaskId(ruleId, targetMonth) {
  const digest = createHash("sha256").update(`${ruleId}:${targetMonth}`).digest("hex").slice(0, 32);
  return `calendar-${digest}`;
}
function taskValues(rule, targetMonth, task) {
  return {
    sourceEventId: task.sourceEventId,
    dateKey: task.dateKey,
    category: task.category,
    label: task.label,
    defaultPlanned: task.defaultPlanned,
    status: task.status,
    details: task.details,
    ruleId: rule.id,
    targetMonth
  };
}
function sameGeneratedTask(current, next) {
  return current.sourceEventId === next.sourceEventId && current.dateKey === next.dateKey && current.category === next.category && current.label === next.label && current.defaultPlanned === next.defaultPlanned && current.status === next.status && JSON.stringify(current.details) === JSON.stringify(next.details);
}
async function upsertGeneratedTask(db, rule, targetMonth, task, requestId) {
  const values = taskValues(rule, targetMonth, task);
  return db.transaction(async (tx) => {
    const currentRows = await tx.select().from(calendarAutoTasks).where(
      and(
        eq2(calendarAutoTasks.ruleId, rule.id),
        eq2(calendarAutoTasks.targetMonth, targetMonth)
      )
    ).limit(1);
    const current = currentRows[0];
    if (current && sameGeneratedTask(current, values)) return "unchanged";
    if (current) {
      const [updated] = await tx.update(calendarAutoTasks).set({ ...values, revision: current.revision + 1 }).where(
        and(
          eq2(calendarAutoTasks.id, current.id),
          eq2(calendarAutoTasks.revision, current.revision)
        )
      ).returning();
      if (!updated) throw new Error("Calendar task changed during synchronization.");
      await tx.insert(auditLogs).values({
        entityType: "calendar_auto_task",
        entityId: updated.id,
        action: "update",
        before: current,
        after: updated,
        actorId: "calendar-automation",
        requestId
      });
      return "updated";
    }
    const [created] = await tx.insert(calendarAutoTasks).values({
      id: generatedTaskId(rule.id, targetMonth),
      ...values,
      revision: 1
    }).onConflictDoNothing().returning();
    if (!created) return "unchanged";
    await tx.insert(auditLogs).values({
      entityType: "calendar_auto_task",
      entityId: created.id,
      action: "create",
      before: null,
      after: created,
      actorId: "calendar-automation",
      requestId
    });
    return "created";
  });
}
function toPersistableTask(rule, decision) {
  return {
    sourceEventId: decision.sourceEventId ?? null,
    dateKey: decision.dateKey ?? "",
    category: rule.task.category,
    label: decision.label ?? rule.task.title,
    defaultPlanned: rule.task.defaultPlanned,
    status: decision.status ?? "scheduled",
    details: {
      finalVisitDate: decision.finalVisitDate ?? "",
      reason: decision.reason ?? ""
    }
  };
}
function missingVisitReview(rule, runDateKey) {
  return {
    sourceEventId: null,
    dateKey: runDateKey,
    category: rule.task.category,
    label: `${rule.task.title}\uFF08\u8A2A\u554F\u4E88\u5B9A\u8981\u78BA\u8A8D\uFF09`,
    defaultPlanned: rule.task.defaultPlanned,
    status: "needs_review",
    details: { reason: "no_matching_visit" }
  };
}
async function findGeneratedTask(db, ruleId, targetMonth) {
  const rows = await db.select().from(calendarAutoTasks).where(
    and(
      eq2(calendarAutoTasks.ruleId, ruleId),
      eq2(calendarAutoTasks.targetMonth, targetMonth)
    )
  ).limit(1);
  return rows[0] ?? null;
}
async function runCalendarTaskSync(options) {
  if (!isCalendarAutomationEnabled()) {
    throw new Error("Calendar automation is disabled.");
  }
  const rules = selectCalendarTaskRules(getCalendarTaskRules(), options.ruleId);
  if (rules.length === 0) {
    throw new Error("The requested calendar automation rule was not found.");
  }
  const db = await getDb();
  if (!db) throw new Error("Database is unavailable for calendar automation.");
  const runDateKey = dateKeyForNow(options.now ?? /* @__PURE__ */ new Date());
  const eventsByCalendar = /* @__PURE__ */ new Map();
  const outcomes = [];
  const diagnostics = [];
  for (const rule of rules) {
    let events = eventsByCalendar.get(rule.calendarId);
    if (!events) {
      events = await readCalendarEvents(rule.calendarId, options.targetMonth);
      eventsByCalendar.set(rule.calendarId, events);
    }
    diagnostics.push(calendarSyncDiagnostic(rule, events, options.targetMonth));
    const decision = evaluateCalendarTaskRule(
      rule,
      events,
      options.targetMonth,
      runDateKey
    );
    const existing = await findGeneratedTask(db, rule.id, options.targetMonth);
    if (decision.kind === "no_matching_visit") {
      if (!existing) {
        outcomes.push({
          ruleId: rule.id,
          action: "skipped",
          reason: "no_matching_visit"
        });
        continue;
      }
      const reviewTask = missingVisitReview(rule, runDateKey);
      if (options.dryRun) {
        outcomes.push({
          ruleId: rule.id,
          action: sameGeneratedTask(existing, taskValues(rule, options.targetMonth, reviewTask)) ? "unchanged" : "updated",
          status: "needs_review"
        });
      } else {
        outcomes.push({
          ruleId: rule.id,
          action: await upsertGeneratedTask(
            db,
            rule,
            options.targetMonth,
            reviewTask,
            options.requestId ?? "calendar-cron"
          ),
          status: "needs_review"
        });
      }
      continue;
    }
    const task = toPersistableTask(rule, decision);
    if (options.dryRun) {
      outcomes.push({
        ruleId: rule.id,
        action: existing ? sameGeneratedTask(existing, taskValues(rule, options.targetMonth, task)) ? "unchanged" : "updated" : "created",
        status: task.status
      });
      continue;
    }
    outcomes.push({
      ruleId: rule.id,
      action: await upsertGeneratedTask(
        db,
        rule,
        options.targetMonth,
        task,
        options.requestId ?? "calendar-cron"
      ),
      status: task.status
    });
  }
  return {
    targetMonth: options.targetMonth,
    dryRun: Boolean(options.dryRun),
    outcomes,
    diagnostics
  };
}

// server/calendarTaskCron.ts
var config = {
  maxDuration: 60
};
var GITHUB_ACTIONS_ISSUER = "https://token.actions.githubusercontent.com";
var GITHUB_ACTIONS_AUDIENCE = "https://github.com/kanoetokyo";
var GITHUB_ACTIONS_REPOSITORY = "kanoetokyo/taskmanager";
var GITHUB_ACTIONS_PREVIEW_WORKFLOW_REF = "kanoetokyo/taskmanager/.github/workflows/run-calendar-preview-sync.yml@refs/heads/main";
var GITHUB_ACTIONS_PRODUCTION_WORKFLOW_REF = "kanoetokyo/taskmanager/.github/workflows/run-calendar-production-sync.yml@refs/heads/main";
var githubActionsJwks = createRemoteJWKSet(
  new URL("https://token.actions.githubusercontent.com/.well-known/jwks")
);
function headerValue(value) {
  return Array.isArray(value) ? value[0] : value;
}
function isCronSecretAuthorized(req) {
  const secret = process.env.CRON_SECRET;
  const authorization = headerValue(req.headers.authorization);
  if (!secret || !authorization) return false;
  const expected = Buffer.from(`Bearer ${secret}`);
  const received = Buffer.from(authorization);
  return expected.length === received.length && timingSafeEqual(expected, received);
}
function isAllowedGitHubActionsSync(payload) {
  const allowedWorkflowRef = process.env.VERCEL_ENV === "preview" ? GITHUB_ACTIONS_PREVIEW_WORKFLOW_REF : process.env.VERCEL_ENV === "production" ? GITHUB_ACTIONS_PRODUCTION_WORKFLOW_REF : null;
  return allowedWorkflowRef !== null && payload.repository === GITHUB_ACTIONS_REPOSITORY && payload.ref === "refs/heads/main" && payload.event_name === "workflow_dispatch" && payload.workflow_ref === allowedWorkflowRef;
}
async function isGitHubActionsAuthorized(req) {
  const authorization = headerValue(req.headers.authorization);
  if (!authorization?.startsWith("Bearer ")) return false;
  try {
    const { payload } = await jwtVerify(
      authorization.slice("Bearer ".length),
      githubActionsJwks,
      {
        issuer: GITHUB_ACTIONS_ISSUER,
        audience: GITHUB_ACTIONS_AUDIENCE
      }
    );
    return isAllowedGitHubActionsSync(payload);
  } catch {
    return false;
  }
}
async function isAuthorized(req) {
  return isCronSecretAuthorized(req) || await isGitHubActionsAuthorized(req);
}
function sendJson(res, statusCode, body) {
  res.statusCode = statusCode;
  res.setHeader("Content-Type", "application/json; charset=utf-8");
  res.setHeader("Cache-Control", "no-store");
  res.end(JSON.stringify(body));
}
function requestUrl(req) {
  const host = headerValue(req.headers.host) ?? "localhost";
  return new URL(req.url ?? "/", `https://${host}`);
}
async function handler(req, res) {
  if (req.method !== "GET") {
    res.setHeader("Allow", "GET");
    sendJson(res, 405, { error: "Method not allowed" });
    return;
  }
  if (!await isAuthorized(req)) {
    sendJson(res, 401, { error: "Unauthorized" });
    return;
  }
  if (!isCalendarAutomationEnabled()) {
    sendJson(res, 503, { error: "Calendar automation is disabled" });
    return;
  }
  const url = requestUrl(req);
  const requestedMonth = url.searchParams.get("month");
  if (requestedMonth && !/^\d{4}-\d{2}$/.test(requestedMonth)) {
    sendJson(res, 400, { error: "Invalid target month" });
    return;
  }
  const requestedRuleId = url.searchParams.get("ruleId");
  if (requestedRuleId && !/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/.test(requestedRuleId)) {
    sendJson(res, 400, { error: "Invalid calendar automation rule" });
    return;
  }
  try {
    const result = await runCalendarTaskSync({
      targetMonth: requestedMonth ?? getDefaultSyncMonth(),
      dryRun: url.searchParams.get("dryRun") === "1",
      requestId: headerValue(req.headers["x-vercel-id"]) ?? "calendar-cron",
      ruleId: requestedRuleId ?? void 0
    });
    const counts = result.outcomes.reduce(
      (summary, outcome) => {
        summary[outcome.action] = (summary[outcome.action] ?? 0) + 1;
        return summary;
      },
      {}
    );
    sendJson(res, 200, {
      targetMonth: result.targetMonth,
      dryRun: result.dryRun,
      counts,
      ...result.dryRun ? { diagnostics: result.diagnostics } : {}
    });
  } catch (error) {
    console.error("[Calendar automation] Synchronization failed", error);
    sendJson(res, 500, { error: "Calendar synchronization failed" });
  }
}
export {
  config,
  handler as default,
  isAllowedGitHubActionsSync
};
